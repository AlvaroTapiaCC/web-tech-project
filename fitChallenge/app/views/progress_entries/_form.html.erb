<%= form_with(model: progress_entry) do |form| %>
  <% if progress_entry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(progress_entry.errors.count, "error") %> prohibited this progress_entry from being saved:</h2>

      <ul>
        <% progress_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
