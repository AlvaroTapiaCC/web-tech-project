<div class="container py-4">
    <h1 class="mb-4">Edit Badge</h1>
    <div class="card">
        <div class="card-body">
            <%= render "form", badge: @badge %>
        </div>
    </div>
    <div class="mt-3 d-flex gap-2">
            <%= link_to "Cancel", @badge, class: "btn btn-secondary" %>
    </div>
</div>
